# v1.0.1

Small cleanup release after the first stable tag.

## Changes

- Cleaned up the Mininet topology script.
- Added short Mininet usage notes to the README and testing docs.
- Split client packet preparation into a helper function.
- Split small server packet helpers for ACK, FIN-ACK, and first-packet parsing.
- Added a unit test for packet creation and first-packet parsing.

## Verification

- `python -m unittest discover -s tests`
- `python -m compileall src tests`
- Local transfer with MD5 comparison
- Verbose transfer with MD5 comparison
- Packet-drop transfer with retransmission and MD5 comparison
